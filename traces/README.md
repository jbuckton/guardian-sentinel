# traces/

Versioned CAN trace library recorded from the Orion BMS Jr 2 unit, stored in the ingestion-log event format (ADR-004/ADR-005) so every trace is directly replayable through the production decoder path. Populated during Phase 2. Empty for now.
